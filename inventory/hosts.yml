# Every YAML file should start with three dashs
---
all:
  hosts:
    gate:
  vars:
    ansible_host: 10.168.0.10
    ansible_user: root
    ansible_become_method: sudo



# Every YAML file should end with three dots
...